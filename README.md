# RK3506 U-Boot Based on the version from Lyra SDK

基于Luckfox Lyra Pi提供的SDK中的u-boot代码，经调整适用于ELIP4NG项目在Luckfox Core3506核心板上使用的u-boot。

加入了基于justfile的构建系统，其部分脚本代码和文档由AI生成，使用前请进行必要的审计。

原始U-boot文档见 `README` 文件。

构建工具文档（A生成）见 `BUILD_TOOL_DOCS.md` 文件。

关于修改后的autoboot相关信息文档（AI生成）见 `AUTO_BOOT_SETUP.md` 文件。